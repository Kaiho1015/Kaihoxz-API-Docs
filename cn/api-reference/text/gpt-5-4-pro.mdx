---
title: "gpt-5.4-pro"
description: "输入输出两项计价、已验收能力与调用示例"
---

{/* Generated by phase3-two-rate-docs.py; model=gpt-5.4-pro */}

使用 `gpt-5.4-pro` 调用 `POST /v1/responses`。下列能力以此型号的验收记录为准。

官方发布日期：[2026-03-05](https://developers.openai.com/api/docs/changelog)。

## 价格与计费

以下是 WaveAPI 售价，单位 **USD / 100 万 token**。官方价格仅作为对照来源，不代表该渠道提供官方所有计费功能。

[官方价格来源](https://developers.openai.com/api/docs/pricing)

| 输入总 token 数 | 输入单价 | 输出单价 |
|---|---:|---:|
| ≤ 272,000 | 27 | 162 |
| > 272,000 | 54 | 243 |

输入总量超过 272,000 token 时，整单输入和输出使用高档价格；不是只给超出部分加价。这个阈值是当前计价配置，不是模型上下文上限，也不表示已进行同等长度的真实调用。

输入总 token 包括响应中报告的缓存统计，均按普通输入单价计费；缓存统计是输入的组成部分，不再重复相加。本接入仅提供输入、输出两项计价，不提供独立缓存折扣、缓存创建或存储能力。

缓存相关请求参数尚未验收，示例不使用这些参数。

`output_tokens` 已包含其明细里的思考 token，不要再次相加。缺少 usage 不代表零费用。

```text
USD = (total_input_tokens * applicable_input_rate
     + output_tokens * applicable_output_rate) / 1,000,000
```

上述公式以完整 usage 为前提。网关先换算基础 quota；启用单请求最小额度规则时，有正用量且计算结果不足 1 基础 quota 的请求先按 1 计，再应用账户分组倍率并取整数。当前验收使用的标准分组倍率为 1，因此正用量单次最低计费 1 quota。1 quota 对应的美元金额由网关换算配置决定，不是固定美元价格；极小请求的实际扣费可能高于公式中的未取整金额。

从 Responses 的 `usage` 读取 token 计数，金额以控制台用量记录和账本核对。

## 已验收范围

账户钱包、本人 Key、用量日志与账务记录的一致性校验通过。下列仅为指定验收样本，不代表该型号全部能力或生产部署均已验证。

- Responses 非流式文本
- Responses 流式文本

流式计费使用 `response.completed`、`response.incomplete`、`response.failed` 或 `response.cancelled` 中可靠的终态 `usage`。失败或截断的响应仍可能产生 token 费用。只有 HTTP 200、`[DONE]` 或部分文本不足以证明完整用量；缺少可靠终态用量时，已预扣额度会保留为待核对状态，先查看已有账务记录，不要自动重发。


未列出的能力不由本页承诺；官方功能或 capabilities 声明不会自动成为本渠道的已验收能力。函数调用验收仅表示参数生成，不表示已执行调用方工具。

## 调用示例

将 `WAVE_API_KEY` 设为本人 Key。示例明确设置非流式和 512 token 请求预算，不额外设置推理参数。

```bash
curl http://localhost:40081/v1/responses \
  -H "Authorization: Bearer $WAVE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "gpt-5.4-pro",
  "input": "Reply with one short greeting.",
  "stream": false,
  "max_output_tokens": 512
}'
```

按实际部署替换网关地址。`max_output_tokens=512` 是示例请求参数，不是上下文/输出能力上限，也不是总费用保证。连接中断或缺少最终 usage 时，先检查已有用量记录，勿据此自动重发。

[Key 鉴权](/cn/docs/authentication)
