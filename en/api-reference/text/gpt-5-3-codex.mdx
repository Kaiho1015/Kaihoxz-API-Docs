---
title: "gpt-5.3-codex"
description: "Input/output pricing, verified capabilities, and request example"
---

{/* Generated by phase3-two-rate-docs.py; model=gpt-5.3-codex */}

Use `gpt-5.3-codex` with `POST /v1/responses`. The scope below follows this model's acceptance records.

Official release date: [2026-02-24](https://developers.openai.com/api/docs/changelog).

## Pricing and billing

These are WaveAPI sale prices in **USD per 1 million tokens**. The official reference does not establish support for every official billing feature on this route.

[Official pricing source](https://developers.openai.com/api/docs/models/gpt-5.3-codex.md)

| Billing dimension | WaveAPI rate |
|---|---:|
| Total input tokens | 1.575 |
| Output tokens | 12.6 |

Total input includes any reported cache statistics and is billed at the ordinary input rate. Cache statistics are part of input, not an extra amount to add again. This integration offers only input and output billing, with no separate cache discount, cache creation, or storage capability.

Cache-related request parameters are unverified and are omitted from the example.

`output_tokens` already includes reasoning tokens in its detail fields; do not add them again. Missing usage does not mean zero cost.

```text
USD = (total_input_tokens * applicable_input_rate
     + output_tokens * applicable_output_rate) / 1,000,000
```

The formula requires complete usage. The gateway converts it to base quota; with the per-request minimum enabled, a request with positive usage and less than 1 base quota is raised to 1 before applying the account group multiplier and taking the integer part. The verified standard group uses multiplier 1, so its minimum charge for positive usage is 1 quota per request. The USD value of 1 quota depends on gateway conversion settings. A very small request can therefore cost more than the unrounded formula amount.

Read token counts from Responses `usage`; reconcile monetary charges with console usage records and the ledger.

## Verified scope

The account wallet, owned key, usage logs, and ledger reconciliation passed. The following scope covers the recorded test samples, not every model feature or proof of production deployment.

- Non-streaming Responses text
- Streaming Responses text

Streaming billing uses reliable terminal `usage` from `response.completed`, `response.incomplete`, `response.failed`, or `response.cancelled`. Failed or incomplete responses can still incur token charges. HTTP 200, `[DONE]`, or partial text alone is insufficient usage evidence. If terminal usage is missing, the reservation remains pending reconciliation; inspect existing billing records before retrying.


This page makes no claim for unlisted capabilities. Official features or capability declarations do not automatically establish verified route support. Function-call checks cover argument generation, not execution of caller tools.

## Request example

Set `WAVE_API_KEY` to your own key. This example explicitly selects non-streaming and a requested 512-token budget, without adding a reasoning parameter.

```bash
curl http://localhost:40081/v1/responses \
  -H "Authorization: Bearer $WAVE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "gpt-5.3-codex",
  "input": "Reply with one short greeting.",
  "stream": false,
  "max_output_tokens": 512
}'
```

Replace the gateway URL for your deployment. `max_output_tokens=512` is an example request parameter, not a model limit or total-cost guarantee. If the connection drops or final usage is absent, inspect existing usage records before retrying.

[Key authentication](/en/docs/authentication)
