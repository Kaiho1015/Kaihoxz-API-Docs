---
title: "Text Models Overview"
description: "Catalog of text models with pricing, verified capabilities and the shared billing rules"
---

Every text model on WaveAPI is served through the OpenAI-compatible Chat Completions endpoint: switch models by changing `model` — no per-vendor URLs, keys or upstream knowledge required. This page is the catalog for text: call entry points first, then the billing rules, then model IDs, prices and verified capabilities grouped by vendor.

## Entry points

<CardGroup cols={2}>
  <Card title="Chat Completions" icon="comments" href="/en/api-reference/text/general-chat">
    `POST /v1/chat/completions`. The default entry; every model on this page is called here, except the Responses-only models.
  </Card>
  <Card title="Responses" icon="layer-group" href="/en/api-reference/text/openai-multimodal">
    `POST /v1/responses`. GPT Pro / Codex / o3-pro models accept only this entry.
  </Card>
  <Card title="Claude Messages" icon="message" href="/en/api-reference/text/claude-messages">
    `POST /v1/messages`. Native envelope kept for apps already built on the Anthropic SDK.
  </Card>
  <Card title="Gemini native" icon="google" href="/en/api-reference/text/gemini-native">
    `POST /v1beta/models/{model}:generateContent`. Native envelope kept for existing Gemini clients.
  </Card>
</CardGroup>

New integrations should use Chat Completions; the native entries only save migration work for existing clients and expose no extra capability. All entries authenticate with a WaveAPI key (Chat / Responses via `Authorization: Bearer`, Messages via `x-api-key`, Gemini native via `x-goog-api-key`); an ecosystem login token is not a model key — see [Authentication](/en/docs/authentication).

## Billing rules

<Note>
**This documentation does not list unit prices.** Rates change upstream, and a number written into docs drifts out of date; this page covers the **billing dimensions and semantics** instead — what gets counted, how tiers switch, what replaces what.

For live rates: `price_config` on `GET /v1/models`, or the console's Model Market.
For what a specific call cost: `usage.cost` in the response (an integer quota) and your billing records.
</Note>

These rules apply to every text model on this page; model-specific differences live in the Notes column and the paragraph under each vendor table.

- **Where rates come from**: 10% off the official list price or equal to it, never above; Batch, Flex and Priority prices are not used. For the actual numbers, read `price_config` from `GET /v1/models`.
- **Total input includes cache**: `prompt_tokens` is total input; reported cache-hit / cache-write tokens are billed at their cache rates, the rest at the ordinary input rate. Cache tokens are part of the total, never an amount added on top.
- **Reasoning is inside output**: `completion_tokens` already includes `completion_tokens_details.reasoning_tokens`; reasoning is billed once at the output rate.
- **Two-rate models**: models whose cache-read column shows `—` bill input and output only; any cache statistics in the response are charged at the ordinary input rate, and explicit `cache_control` is rejected before reservation.
- **Long-context tiers**: a tiered model picks its tier from **total input (cache included)**; once the threshold is reached the whole request — input, cache and output — moves to the higher rate, not just the excess. Thresholds and how the exact boundary value is treated are stated per vendor.
- **Time-of-day pricing**: DeepSeek V4 is priced by the UTC slot at request start; see the DeepSeek section.
- **Quota conversion**: the line items are summed and converted to an integer quota at **500,000 quota = 1 USD**; a request with positive usage that rounds below 1 quota is charged 1, then the account group multiplier is applied and the result truncated. `usage.cost` in the response is that integer quota, **not dollars**.

All rates are in USD per 1 million tokens:

```text
ordinary input tokens = prompt_tokens - cached_tokens - cache_write_tokens
cost (USD) = ( ordinary input tokens × input rate
             + cached_tokens × cache-read rate
             + cache_write_tokens × cache-write rate
             + completion_tokens × output rate ) / 1,000,000
quota      = cost (USD) × 500,000, truncated, minimum 1
```

The final charge is the text ledger entry in your billing records; its states mean:

| Ledger state | Meaning |
|---|---|
| `reserved` | Reserved, result not yet confirmed; the reservation is not the final charge |
| `pricing_pending` | Usage received but price or metering needs review; reservation held, no final cost published |
| `settlement_pending` / `refund_pending` | Waiting for settlement or refund to complete |
| `settled` / `refunded` | Charge or refund completed; reconcile against the final ledger amount |

<Warning>
A missing final `usage`, a dropped connection or a response without a cost field **does not make the call free**. Check the existing usage record in the console before resubmitting; never retry automatically on that basis.
</Warning>

## Caching

Caching is graded per model by which cache rates the model group has configured in
`price_config` — **never inferred from what the vendor supports**. Cache reads and writes
**replace** the matching ordinary input charge at their own rate; they are not added on top.

| Grade | Test | Models |
|---|---|---|
| No cache discount | `cache_billing: "input_output"` | every other model in the catalog; any cache statistics in the response bill at the ordinary input rate |
| Cache read only | has `cache_read` | `gpt-5.5` `gpt-5.4` `grok-4.6` `gemini-3.6-flash` `gemini-3.7-flash` `gemini-3.8-flash` `kimi-k3` `qwen3.8-max` |
| Cache read + write | also `cache_write` | `gpt-5.6-luna` `gpt-5.6-terra` `gpt-5.6-sol` `gpt-6-astra` |
| Cache read + 5-min write + 1-hour write | also `cache_write_1h` | `claude-opus-5` `claude-sonnet-5` `claude-fable-5` `claude-fable-5-1` |
| Time-of-day cache rate | `cache_read` inside each `text_schedule` window | `deepseek-v4-pro` `deepseek-v4-flash` |

### Automatic vs explicit caching

**Automatic caching** is decided upstream and needs no parameters from you: hit tokens appear in
`usage.prompt_tokens_details.cached_tokens` and bill at that model's cache-read rate. Hits are never
guaranteed — do not budget as if every call will hit.

**Explicit caching** marks a reusable prefix with `cache_control`, and the gateway allows it per model:

| Models | Explicit `cache_control` |
|---|---|
| `claude-opus-5` `claude-sonnet-5` `claude-fable-5` `claude-fable-5-1` | ✅ Allowed; all three cache rates configured (read / 5-minute write / 1-hour write) |
| every `gemini-*`, and `qwen3.8-max` | ❌ Rejected before reservation: explicit and automatic cache reads carry different rates upstream and both billing dimensions are not configured yet, so only automatic caching is open |
| two-rate models | ❌ Rejected before reservation: no cache billing dimension exists |
| everything else (`gpt-5.4/5.5/5.6-*`, `gpt-6-astra`, `grok-4.6`, `kimi-k3`, `deepseek-v4-*`) | Upstream caches automatically; `cache_control` is neither needed nor used |

A rejected request returns HTTP 400 **before any quota is reserved, so it is not charged**.

<Note>
The billing dimensions for explicit caching are configured and the gateway allows it, but
**cache hits and TTL writes have not been through acceptance testing yet**. On first integration,
check the cache line items in your console usage record on a small request before scaling up.
</Note>

### Writing a Claude explicit cache request

On the OpenAI-compatible endpoint, `content` **must be an array of content blocks**. A string
`content` cannot carry `cache_control`; the marker is ignored and the whole prefix bills as ordinary input.

```json
{
  "model": "claude-sonnet-5",
  "max_tokens": 1024,
  "messages": [
    {
      "role": "system",
      "content": [
        {
          "type": "text",
          "text": "Put the long prefix you want to reuse here...",
          "cache_control": {"type": "ephemeral"}
        }
      ]
    },
    {"role": "user", "content": "Answer using the content above."}
  ]
}
```

The native [Messages endpoint](/en/api-reference/text/claude-messages) takes the same shape with the
prefix in the `system` array. `ttl` defaults to 5 minutes; for the 1-hour tier send
`"cache_control": {"type": "ephemeral", "ttl": "1h"}` together with the
`anthropic-beta: extended-cache-ttl-2025-04-11` header — the gateway forwards that header upstream as-is.

### Hit conditions and reading usage

- **The prefix must be long enough** — usually at least ~1,024 tokens; short prefixes create no cache.
- **The prefix must be byte-identical** — any difference in text, spaces, newlines or content-block order misses.
- **It must be within the TTL** — 5 minutes or 1 hour, counted from the most recent hit.

In Chat responses the hit count is `usage.prompt_tokens_details.cached_tokens`. Native Messages reports
`cache_read_input_tokens` and `cache_creation_input_tokens` instead, and its `input_tokens` **excludes**
both — total input is the sum of the three (whereas Chat's `prompt_tokens` already includes them).
Written tokens bill at the cache-write rate, which is higher than ordinary input, so explicit caching
only pays off when the prefix is stable and gets reused within the TTL.

## Not yet available

The following are rejected **before any quota is reserved** (HTTP 400, no charge), because their extra charge dimensions are not yet represented in the accepted text price contract:

- **Vendor built-in tools** — any entry in `tools` whose type is not `function` or `custom` (hosted web search, hosted execution, and similar). Client-executed function tools are not in this group and work normally.
- **`web_search_options`**.
- **`service_tier`** set to anything other than `standard` / `default`.
- **Explicit caching** — see the table in the previous section.

Model groups also declare the protocols and capabilities this route has actually been verified on: a request arriving on a protocol the group never declared (Chat / Responses / Claude Messages / Gemini native), or `tools`, `tool_choice` or `response_format` sent to a model declared not to support them, is rejected before reservation as well. What each model supports is the catalog above.

## Model catalog

<Note>
**How to read the tables**: capabilities shown are samples **actually accepted on this route**; anything the vendor supports but this route has not accepted is left out. **Non-streaming text and streaming output (with the final usage frame) are verified on every model**, so they are not given a column. In the JSON / Tools column the left mark is JSON Schema structured output and the right one is function-call argument generation (**the gateway executes nothing** — your application runs the tool); `✓` means accepted, `—` means not accepted or explicitly disabled. Other accepted items such as cache hits and native protocols appear in the model notes below each table. The Billing column names which **billing dimensions** a model has (whether cache is priced, how many cache tiers, whether it is time-of-day) and **carries no rates** — for those see [Catalog API](#catalog-api). For a model with a long-context tier, the threshold is judged on total input (cache included) and switches the whole request; the exact thresholds are in the model notes under each table. Model notes also carry route-declared limits and per-model restrictions — a route-declared limit is not a stress-test result and does not promise using input and output to the maximum at once.
</Note>

### OpenAI

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `gpt-6-astra` | + cache read/write | ✓ / — |
| `gpt-5.6-luna` | + cache read/write | ✓ / ✓ |
| `gpt-5.6-terra` | + cache read/write | ✓ / ✓ |
| `gpt-5.6-sol` | + cache read/write | ✓ / ✓ |
| `gpt-5.5` | + cache read | ✓ / ✓ |
| `gpt-5.4` | + cache read | ✓ / ✓ |
| `gpt-5.4-pro` | input + output | — / — |
| `gpt-5.4-mini` | input + output | ✓ / ✓ |
| `gpt-5.4-nano` | input + output | ✓ / ✓ |
| `gpt-5.3-codex` | input + output | — / — |
| `gpt-5.2` | input + output | ✓ / ✓ |
| `gpt-5.2-pro` | input + output | — / — |
| `gpt-5.1` | input + output | ✓ / ✓ |
| `gpt-5` | input + output | — / ✓ |
| `gpt-5-mini` | input + output | ✓ / ✓ |
| `gpt-5-nano` | input + output | ✓ / ✓ |
| `gpt-5-pro` | input + output | — / — |
| `gpt-4.1` | input + output | ✓ / ✓ |
| `gpt-4.1-mini` | input + output | ✓ / ✓ |
| `gpt-4.1-nano` | input + output | ✓ / ✓ |
| `o3` | input + output | ✓ / ✓ |
| `o3-pro` | input + output | — / — |
| `o4-mini` | input + output | ✓ / ✓ |
| `o3-mini` | input + output | ✓ / ✓ |
| `o1` | input + output | ✓ / ✓ |

**Model notes**

- `gpt-6-astra` — 272K long-context tier; has a separate cache-write rate; route-declared limits 922K in / 12.8K out; tools not enabled (OpenAI routes Astra tools via Responses; not accepted on this route); accepted with `reasoning_effort: "low"`; `none` unsupported; do not send temperature / top_p / logprobs; cache and long-context tiers priced but not exercised
- `gpt-5.6-luna` — 272K long-context tier; has a separate cache-write rate; route-declared limits 922K in / 128K out; cache write, cache hit and a long-context request above 272K all exercised
- `gpt-5.6-terra` — 272K long-context tier; has a separate cache-write rate; route-declared limits 922K in / 12.8K out
- `gpt-5.6-sol` — 272K long-context tier; has a separate cache-write rate; route-declared limits 922K in / 12.8K out; official baseline is promotional pricing guaranteed through 2026-11-21; re-check then
- `gpt-5.5` — 272K long-context tier; route-declared limits 1M in / 12.8K out; no separate cache-write rate
- `gpt-5.4` — 272K long-context tier; route-declared limits 1M in / 12.8K out; no separate cache-write rate
- `gpt-5.4-pro` — 272K long-context tier; `/v1/responses` only; text and streaming are the accepted scope there
- `gpt-5.3-codex` — `/v1/responses` only; text and streaming are the accepted scope there
- `gpt-5.2-pro` — `/v1/responses` only; text and streaming are the accepted scope there
- `gpt-5-pro` — `/v1/responses` only; text and streaming are the accepted scope there
- `o3-pro` — `/v1/responses` only; text and streaming are the accepted scope there

GPT-5 and later use `max_completion_tokens` to cap output and `reasoning_effort` to pick a reasoning tier (acceptance used `"none"`; GPT-6 Astra was accepted with `"low"` only). `max_tokens` is rejected. `gpt-5-pro`, `gpt-5.2-pro`, `gpt-5.4-pro`, `gpt-5.3-codex` and `o3-pro` declare only the `openai_responses` protocol: they are served on the [Responses API](/en/api-reference/text/openai-multimodal) and rejected before reservation on Chat. The 272K tier is judged on **total input**: exactly 272,000 stays on the base rate, anything above moves the whole request (cache and output included) to the long-context rate. GPT-5.6 cache writes cost 1.25× the tier's input rate and replace the ordinary input charge rather than adding to it; GPT-5.4 / 5.5 have no separate cache-write rate.

### Anthropic

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `claude-fable-5-1` | + 3 cache rates | — / ✓ |
| `claude-fable-5` | + 3 cache rates | — / ✓ |
| `claude-opus-5` | + 3 cache rates | — / ✓ |
| `claude-sonnet-5` | + 3 cache rates | — / ✓ |
| `claude-opus-4-8` | input + output | — / ✓ |
| `claude-opus-4-7` | input + output | — / ✓ |
| `claude-opus-4-6` | input + output | — / ✓ |
| `claude-opus-4-5` | input + output | — / ✓ |
| `claude-sonnet-4-6` | input + output | — / ✓ |
| `claude-sonnet-4-5` | input + output | — / ✓ |
| `claude-haiku-4-5` | input + output | — / ✓ |

**Model notes**

- `claude-fable-5-1` — cache writes are priced at two TTLs, 5-minute and 1-hour; route-declared limits 1M in / 128K out; JSON Schema not enabled (returned Markdown-wrapped text in tests); tools accept only `tool_choice: "auto"`; required / named choices are rejected upstream; cache rates configured; hits and TTL writes not accepted yet
- `claude-fable-5` — cache writes are priced at two TTLs, 5-minute and 1-hour; route-declared limits 1M in / 128K out; JSON Schema not enabled; tools accept only `tool_choice: "auto"`; cache rates configured, not accepted yet
- `claude-opus-5` — cache writes are priced at two TTLs, 5-minute and 1-hour; route-declared limits 128K in / 16K out; JSON Schema / json_object rejected before reservation; native `/v1/messages` available (only this and one other Claude 5 model declare that protocol); cache rates configured, not accepted yet
- `claude-sonnet-5` — cache writes are priced at two TTLs, 5-minute and 1-hour; route-declared limits 1M in / 128K out; JSON Schema / json_object rejected before reservation; native `/v1/messages` available (only this and one other Claude 5 model declare that protocol); cache rates configured, not accepted yet

Use `max_tokens` on the Chat endpoint; do not send the GPT-only `reasoning_effort` / `max_completion_tokens`. The four Claude 5 / Fable models carry three cache rates (read / 5-minute write / 1-hour write) and are the only models that currently **allow explicit `cache_control`** — see [Caching](#caching) for the request shape. **Structured output is not enabled** — tests did not follow the schema, so `response_format` with `json_schema` / `json_object` is rejected before reservation. Fable models accept only `tool_choice: "auto"`; `required` or a named function returns 400 upstream (the reservation is fully refunded). The 4.x line bills input / output only and rejects `cache_control` before reservation. Apps built on the Anthropic SDK can use the native [Messages endpoint](/en/api-reference/text/claude-messages), but **only `claude-sonnet-5` and `claude-opus-5` declare the `anthropic_messages` protocol**; any other Claude model sent to `/v1/messages` is rejected before reservation — use the Chat endpoint instead.

### Google Gemini

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `gemini-3.8-flash` | + cache read | ✓ / ✓ |
| `gemini-3.7-flash` | + cache read | ✓ / ✓ |
| `gemini-3.6-flash` | + cache read | ✓ / ✓ |
| `gemini-3.5-flash` | input + output | ✓ / ✓ |
| `gemini-3.5-flash-lite` | input + output | ✓ / ✓ |
| `gemini-3.1-pro-preview` | input + output | ✓ / — |
| `gemini-3-flash-preview` | input + output | ✓ / ✓ |
| `gemini-2.5-pro` | input + output | ✓ / ✓ |
| `gemini-2.5-flash-lite` | input + output | ✓ / ✓ |

**Model notes**

- `gemini-3.8-flash` — route-declared limits 1,048,576 in / 65,536 out; native protocol verified; implicit cache-read rate; hits decided upstream
- `gemini-3.7-flash` — route-declared limits 1,048,576 in / 65,536 out; native protocol verified; implicit cache-read rate; hits decided upstream
- `gemini-3.6-flash` — route-declared limits 1,048,576 in / 65,536 out; native protocol verified; cache hit not accepted yet
- `gemini-3.5-flash` — native protocol verified
- `gemini-3.1-pro-preview` — 200K long-context tier; native protocol verified
- `gemini-3-flash-preview` — native protocol verified
- `gemini-2.5-pro` — 200K long-context tier; native protocol verified
- `gemini-2.5-flash-lite` — native protocol verified

Besides the OpenAI-compatible endpoint, these models also declare the Gemini native protocol: `gemini-2.5-flash-lite`, `gemini-2.5-pro`, `gemini-3-flash-preview`, `gemini-3.1-pro-preview`, `gemini-3.5-flash`, `gemini-3.6-flash`, `gemini-3.7-flash`, `gemini-3.8-flash` (`gemini-3.5-flash-lite` is Chat-only). The native entry is `POST /v1beta/models/{model}:generateContent` (streaming: `:streamGenerateContent?alt=sse`), see [Gemini native API](/en/api-reference/text/gemini-native). Native responses report usage in `usageMetadata` without `usage.cost`; `candidatesTokenCount` and `thoughtsTokenCount` are reported separately and billed output is their sum. The Pro models (`gemini-2.5-pro` / `gemini-3.1-pro-preview`) have a 200K long-context tier that switches the whole request above 200,000 total input. 3.6 / 3.7 / 3.8 Flash have an implicit cache-read rate whose hits are decided upstream; the other models bill two rates. Explicit caching (`cache_control` / `cachedContent`) is rejected before reservation **across the whole Gemini line** — only automatic caching is open. Built-in tools, `web_search_options` and any `service_tier` other than `standard` / `default` are rejected as well.

### DeepSeek

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `deepseek-v4-pro` | time-of-day | ✓ / ✓ |
| `deepseek-v4-flash` | time-of-day | ✓ / ✓ |
| `deepseek-r1-0528` | input + output | ✓ / ✓ |
| `deepseek-v3.2` | input + output | ✓ / ✓ |
| `deepseek-v3.2-exp` | input + output | — / ✓ |
| `deepseek-v3.1-terminus` | input + output | — / ✓ |

**Model notes**

- `deepseek-v4-pro` — route-declared limits 1M in / 393K out; time-of-day pricing, see the table at the end of this section; accepted with `thinking.type=disabled`; cache hit exercised at 1,024 tokens: the same input dropped from 354 to 27 quota
- `deepseek-v4-flash` — route-declared limits 128K in / 16K out; time-of-day pricing, see the table at the end of this section; accepted with `thinking.type=disabled`; cache rate configured; no hit observed during acceptance
- `deepseek-v3.2-exp` — no strict JSON Schema; separate model from `deepseek-v3.2` (2025-09-29 experimental build)
- `deepseek-v3.1-terminus` — no strict JSON Schema; no longer listed on DeepSeek's main API

V4 Pro / Flash are priced by the **UTC time slot at request start** (Beijing time = UTC+8); reservation, response cost and final settlement use the same frozen rate even if the response crosses a boundary, and a quote reflects the price at the moment it was requested rather than locking a later request. Acceptance sent `thinking: {"type": "disabled"}` explicitly; the default thinking mode is not accepted yet. V4 cache-hit rates replace the matching input charge; there is no separate cache write. V3.x / R1 bill two rates; `deepseek-v3.2-exp` and `deepseek-v3.1-terminus` offer no strict JSON Schema — use `deepseek-v3.2` or `deepseek-r1-0528` when you need schema guarantees.

| Slot (UTC, start inclusive, end exclusive) | Tier |
|---|---|
| Mon–Fri 01:00–04:00, 06:00–10:00 | peak |
| Daily 00:00–14:00 outside the slots above | standard |
| Daily 14:00–24:00 | off-peak (cheapest) |

Beijing time is UTC+8. The per-slot input / output / cache-read rates for Pro and Flash are in
`price_config.text_schedule` on `GET /v1/models` — that is the complete table, and the one settlement actually uses.

### Qwen (Alibaba Cloud)

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `qwen3.8-max` | + cache read | ✓ / ✓ |
| `qwen3.7-max` | input + output | ✓ / ✓ |
| `qwen3.7-plus` | input + output | ✓ / ✓ |
| `qwen3.6-plus` | input + output | — / ✓ |
| `qwen3.6-flash` | input + output | — / ✓ |

**Model notes**

- `qwen3.8-max` — route-declared limits 983K in / 131K out; implicit cache hit exercised but not guaranteed on every call; explicit `cache_control` rejected before reservation; use `tool_choice: "auto"` for tools
- `qwen3.7-plus` — 256K long-context tier
- `qwen3.6-plus` — 256K long-context tier
- `qwen3.6-flash` — 256K long-context tier

Qwen3.8 Max exposes implicit caching only (a hit was exercised, but hits are not guaranteed); explicit `cache_control` is rejected before reservation — explicit and implicit reads carry different rates, and the two are not mixed until both dimensions are configured. Plus / Flash have a 256K tier switched on total input for the whole request. Reasoning consumes the output budget; turning it off is not guaranteed.

### Kimi (Moonshot)

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `kimi-k3` | + cache read | ✓ / ✓ |
| `kimi-k2.7-code` | input + output | ✓ / ✓ |
| `kimi-k2.7-code-highspeed` | input + output | ✓ / ✓ |
| `kimi-k2.6` | input + output | — / ✓ |

**Model notes**

- `kimi-k3` — route-declared limits 1M in / 105K out; cache hit exercised; use `tool_choice: "auto"`; give tool calls ≥512 output budget (128 truncated in tests)

Kimi K3 has a cache-read rate (a 180-token hit was exercised); use `tool_choice: "auto"` and give tool calls enough output budget (128 truncated, 512 passed). K2.x bill two rates.

### Grok (xAI)

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `grok-4.6` | + cache read | ✓ / — |
| `grok-4.5` | input + output | ✓ / — |
| `grok-4.3` | input + output | ✓ / ✓ |
| `grok-4.20-0309-reasoning` | input + output | ✓ / ✓ |
| `grok-4.20-0309-non-reasoning` | input + output | ✓ / ✓ |
| `grok-build-0.1` | input + output | ✓ / ✓ |

**Model notes**

- `grok-4.6` — 200K long-context tier; route-declared limits 500K in / 12.8K out; cache hit exercised; tools not enabled (explicit tool config rejected before reservation); `max_tokens` is not a hard cap; reasoning can push completion_tokens past it
- `grok-4.5` — 200K long-context tier; tools not accepted yet
- `grok-4.3` — 200K long-context tier
- `grok-4.20-0309-reasoning` — 200K long-context tier
- `grok-4.20-0309-non-reasoning` — 200K long-context tier
- `grok-build-0.1` — 200K long-context tier

The whole Grok line has a 200K tier: once total input (cache included) reaches 200,000, input, cache read and output for the entire request move to the higher rate — not just the excess. Grok 4.6 has a cache-read rate with an exercised hit; **tools are not enabled** — explicit tool config is rejected before reservation and forced tool calls returned plain text in tests. `max_tokens` is not a hard cap on reasoning models: `completion_tokens` including reasoning can exceed it, so budget with key quotas.

### GLM (Z.ai)

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `glm-5.2` | input + output | — / ✓ |
| `glm-5.1` | input + output | — / ✓ |
| `glm-5` | input + output | ✓ / ✓ |
| `glm-4.7` | input + output | ✓ / ✓ |
| `glm-4.6` | input + output | — / ✓ |

Two-rate billing; `cache_control` is rejected before reservation.

### MiniMax

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `minimax-m3` | input + output | — / ✓ |
| `minimax-m2.7` | input + output | — / ✓ |
| `minimax-m2.5` | input + output | — / ✓ |
| `minimax-m2.1` | input + output | — / ✓ |

Two-rate billing; `cache_control` is rejected before reservation.

### Others

| Model ID | Billing | JSON / Tools |
|---|---|:---:|
| `mimo-v2.5-pro` | input + output | ✓ / ✓ |
| `step-3.7-flash` | input + output | — / ✓ |

**Model notes**

- `mimo-v2.5-pro` — Xiaomi MiMo
- `step-3.7-flash` — StepFun Step

Two-rate billing; `cache_control` is rejected before reservation.

## Catalog API

Live availability, price configuration and capability flags come from the catalog API; this page lags behind it:

```bash
curl https://www.qingbo.dev/v1/models \
  -H "Authorization: Bearer $WAVE_API_KEY"
```

Single model: `GET /v1/models/{model}`. The `price_config` it returns is where this page's tables come from: `input` / `output` are the base rates, `cache_read` / `cache_write` / `cache_write_1h` show how far caching is opened up for that model, `input_tier_threshold` with the `*_above_price` fields is the long-context tier, and `cache_billing: "input_output"` means input and output are the only two rates.

A model present in an old catalog is not automatically live; models released before September 2024 or already retired are not offered in the new catalog.

